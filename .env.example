# Optional but recommended: get a free key at https://web3forms.com (use zaviahorg@gmail.com)
# Add the same variable in Netlify → Site settings → Environment variables, then redeploy.
VITE_WEB3FORMS_ACCESS_KEY=

# Web3Forms dashboard checklist (cannot be set from this repo):
# 1. Confirm the form access key matches VITE_WEB3FORMS_ACCESS_KEY.
# 2. If on Web3Forms Pro: enable Restrict to Domain / Trusted Domains for: zaviah.org
#    (Do not include https://. Localhost will stop working once this is enabled.)
# 3. Leave hCaptcha/Turnstile off until you intentionally add them to the UI.
# FormSubmit is the silent fallback only — it uses _honey honeypot; captcha is off on purpose.
